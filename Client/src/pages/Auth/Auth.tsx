import { SignupFormDemo } from "../../components/component/SignUpForm";
const Authenticaiton = () => {
  return (
    <div>
      {/*
        User Logged in logic if loged in acces main ui
        if old user
        loginForm Component
        if new user 
      */}
      <SignupFormDemo />
    </div>
  );
};

export default Authenticaiton;
